#include <iostream>
int main(void){
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    int N, X;
    std::cin >> N;
    std::cin >> X;
    int *a = new int[N];
    for(int i = 0; i < N; i++){
        std::cin >> a[i];
        if(a[i] < X)
            std::cout << a[i] << ' ';
    }
}
